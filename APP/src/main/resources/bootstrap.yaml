management:
  server:
    port: 8082
  endpoints:
    web:
      exposure:
        include: "*"
server:
  port: 8082
spring:
  application:
    name: App
  cloud:
    kubernetes:
      discovery:
        enabled: true
  main:
    cloud-platform: kubernetes